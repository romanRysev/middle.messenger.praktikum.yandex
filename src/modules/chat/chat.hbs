<div class="chat">
  <aside class="chat__left-column">
    {{{profileLink}}}
    {{{searchInput}}}
    <hr class="divider_h" />
    <div class="chat__list">
      {{#if this.shortView}}
        {{{shortView}}}
      {{/if}}
      <hr class="divider_h" />
    </div>
    {{{newChatButton}}}
  </aside>
  <section class="chat__right-column">
    {{{addUserButton}}}
    <div class="chat__messages">
      {{#each messages}}
        {{#if this.isMy}}
          <div class="chat__message_my"><p>{{this.content}}</p><small
            >{{this.time}}</small></div>
        {{else}}
          <div class="chat__message_other"><p>{{this.content}}</p><small
            >{{this.time}}</small></div>
        {{/if}}
      {{/each}}</div>
    <form name="newMessage" class="chat__input-form">
      <input
        type="text"
        name="message"
        placeholder="message..."
        class="chat__message-input"
        required
      /><button class="chat__send-button" name="submit"><img
          src="{{sendIconUrl}}"
          alt="send message"
        /></button></form></section>
</div>